rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Voice recordings are stored under:
    //   users/{uid}/entries/{entryId}/audio.m4a
    match /users/{uid}/{path=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}

